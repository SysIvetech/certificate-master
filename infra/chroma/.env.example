# Copy to .env on the Docker host. Keep the current server image until tested.
# Obtain its RepoDigest using docker image inspect <IMAGE_ID>.
CHROMA_IMAGE=chromadb/chroma@sha256:REPLACE_WITH_VERIFIED_DIGEST
CHROMA_DATA_DIR=/data01/chroma-data
CHROMA_BIND_IP=127.0.0.1
CHROMA_PORT=38000
